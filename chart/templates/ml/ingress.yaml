{{- if .Values.ingress.ml.enabled }}
{{- if eq .Values.ingress.controller "gce" }}
  {{- $_ := set . "ml_path" "/*" -}}
{{- else if eq .Values.ingress.controller "ncp" }}
  {{- $_ := set . "ml_path" "/.*" -}}
{{- else }}
  {{- $_ := set . "ml_path" "/" -}}
{{- end }}
{{- $fqdn := include "app.ml.fqdn" . }}
apiVersion: {{ template "common.capabilities.ingress.apiVersion" . }}
kind: Ingress
metadata:
  name: {{ include "app.ml.name" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  annotations:
  {{- if .Values.ml.tls.enabled }}
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
  {{- end }}
  {{- if eq .Values.ingress.controller "ncp" }}
    ncp/use-regex: "true"
    {{- if .Values.ml.tls.enabled }}
    ncp/http-redirect: "true"
    {{- end }}
  {{- end }}
  {{- if .Values.ingress.annotations }}
    {{- include "common.tplvalues.render" (dict "value" .Values.ingress.annotations "context" .) | nindent 4 }}
  {{- end }}
  {{- if .Values.commonAnnotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" .) | nindent 4 }}
  {{- end }}
spec:
  {{- if eq .Values.ingress.controller "ncp" }}
  backend: {{- include "common.ingress.backend" (dict "serviceName" (include "app.ml.name" .) "servicePort" (ternary "https" "http" .Values.ml.tls.enabled) "context" $) | nindent 4 }}
  {{- end }}
  {{- if and .Values.ingress.ingressClassName (eq "true" (include "common.ingress.supportsIngressClassname" .)) }}
  ingressClassName: {{ .Values.ingress.ingressClassName | quote }}
  {{- end }}
  rules:
    {{- if .Values.ingress.ml.hostname }}
    - host: {{ $fqdn | quote }}
      http:
        paths:
          {{- if .Values.ingress.ml.extraPaths }}
          {{- toYaml .Values.ingress.ml.extraPaths | nindent 10 }}
          {{- end }}
          - path: {{ .ml_path }}
            {{- if eq "true" (include "common.ingress.supportsPathType" .) }}
            pathType: {{ .Values.ingress.ml.pathType }}
            {{- end }}
            backend: {{- include "common.ingress.backend" (dict "serviceName" (include "app.ml.name" .) "servicePort" (ternary "https" "http" .Values.ml.tls.enabled) "context" $) | nindent 14 }}
    {{- end }}
  {{- if or (and .Values.ingress.ml.tls (or (include "common.ingress.certManagerRequest" ( dict "annotations" .Values.ingress.ml.annotations )) .Values.ingress.ml.selfSigned)) .Values.ingress.ml.extraTls }}
  tls:
    {{- if and .Values.ingress.ml.tls (or (include "common.ingress.certManagerRequest" ( dict "annotations" .Values.ingress.ml.annotations )) .Values.ingress.ml.selfSigned) }}
    - hosts:
        - {{ $fqdn | quote }}
      secretName: {{ printf "%s-tls" $fqdn }}
    {{- end }}
    {{- if .Values.ingress.ml.extraTls }}
    {{- include "common.tplvalues.render" (dict "value" .Values.ingress.ml.extraTls "context" $) | nindent 4 }}
    {{- end }}
  {{- end }}
{{- end }}
